[project]
name = "clovers-client"
version = "0.1.5"
description = ""
authors = [{ name = "KarisAya", email = "karisaya@foxmail.com" }]
readme = "README.md"
requires-python = ">=3.12,<4.0.0"

dependencies = ["clovers (>=0.4.8,<1.0.0)"]

[project.optional-dependencies]
console = ["pydantic>=2.10.6", "uvicorn>=0.42.0", "fastapi>=0.135.2"]
onebot = ["pydantic>=2.10.6", "httpx>=0.23.1", "websockets>=15.0.0"]
qq = ["pydantic>=2.10.6", "qq-botpy>=1.2.1"]
